{
  "_comment": "Library files. Modes: default (one sidebar entry per <trk>/<rte>), grouped:true (one entry per file), groupByGeomType:true (one entry per geometry type — Lines/Points/Polygons — recommended for files with many features). Click features on the map for individual names regardless of mode.",
  "files": [
    {
      "file": "camping.gpx",
      "name": "Camping"
    },
    {
      "file": "PeaksAndRoutes.gpx",
      "name": "Peaks & Routes",
      "groupByGeomType": true
    }
  ]
}
